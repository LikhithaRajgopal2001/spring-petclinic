apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "my-java-app.fullname" . }}
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: demo.petclinic.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "my-java-app.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
